<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.api.flobankapi.mapper.admin.SurveyAdminMapper">

    <insert id="insertSurvey" useGeneratedKeys="true" keyProperty="surveyId" keyColumn="SURVEY_ID">
        INSERT INTO TB_SURVEY (
            TITLE,
            DESCRIPTION,
            IS_ACTIVE,
            CREATED_BY,
            UPDATED_BY
        ) VALUES (
            #{title},
            #{description},
            #{isActive},
            #{createdBy},
            #{updatedBy}
        )
    </insert>

    <insert id="insertQuestion" useGeneratedKeys="true" keyProperty="qId" keyColumn="Q_ID">
        INSERT INTO TB_SURVEY_Q (
            SURVEY_ID,
            Q_NO,
            Q_KEY,
            Q_TEXT,
            Q_TYPE,
            IS_REQUIRED,
            MAX_SELECT,
            IS_ACTIVE,
            CREATED_BY,
            UPDATED_BY
        ) VALUES (
            #{surveyId},
            #{qNo},
            #{qKey},
            #{qText},
            #{qType},
            #{isRequired},
            #{maxSelect},
            #{isActive},
            #{createdBy},
            #{updatedBy}
        )
    </insert>

    <insert id="insertOption" useGeneratedKeys="true" keyProperty="optId" keyColumn="OPT_ID">
        INSERT INTO TB_SURVEY_OPT (
            Q_ID,
            OPT_CODE,
            OPT_TEXT,
            OPT_VALUE,
            OPT_ORDER,
            IS_ACTIVE,
            CREATED_BY,
            UPDATED_BY
        ) VALUES (
            #{qId},
            #{optCode},
            #{optText},
            #{optValue},
            #{optOrder},
            #{isActive},
            #{createdBy},
            #{updatedBy}
        )
    </insert>

    <select id="selectSurveyList" resultType="kr.co.api.flobankapi.dto.admin.survey.SurveySummaryDTO">
        SELECT
            SURVEY_ID AS surveyId,
            TITLE,
            DESCRIPTION,
            IS_ACTIVE AS isActive,
            CREATED_AT AS createdAt
        FROM TB_SURVEY
        ORDER BY SURVEY_ID DESC
    </select>

</mapper>
